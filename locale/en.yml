afrux-forum-widgets-core:
  admin:
    cache_not_writable_warning: Cache is currently not writable, this will prevent certain widgets from working. Please fix your storage directory's permissions.
    settings:
      prefer_data_with_initial_load: Prefer loading all widgets data with initial page response.
      prefer_data_with_initial_load_help: Instead of loading widget data after initial page load by sending API requests, the data will be (when possible) loaded with the inital load.
